import { Button as storyButton } from "../../../../elements/Button/index";
import styled from "@emotion/styled";
import { COLORS } from "../../../../style/theme";
COLORS;
export const StyledButton = styled(storyButton)`
  background: ${COLORS.BRAND_1};
`;
